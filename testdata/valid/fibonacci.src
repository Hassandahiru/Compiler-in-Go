package main

// Fibonacci function - computes the nth Fibonacci number
func fibonacci(n int) int {
    if (n <= 1) {
        return n;
    }
    return fibonacci(n - 1) + fibonacci(n - 2);
}

// Main entry point
func main() {
    var result int = fibonacci(10);
    var x int = 5;
    var y int = x * 2 + 3;
}
